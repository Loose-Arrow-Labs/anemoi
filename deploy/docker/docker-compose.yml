services:
  anemoi-daemon:
    build:
      context: ../..
      dockerfile: deploy/docker/Dockerfile
    ports:
      - "7070:7070"
    environment:
      ANEMOI_BIND: "0.0.0.0:7070"
      ANEMOI_CONFIG: "/app/config/anemoi.example.yaml"
    volumes:
      - ../../config:/app/config:ro
